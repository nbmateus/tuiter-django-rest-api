django
djangorestframework
django-rest-auth
django-allauth
mysqlclient


APPS:
    ACCOUNTS:
        MODELS:
            User:
                username
                email
                password
                is_admin
            
            Profile:
                fullname
                description
                pfp
                birthdate
                followers
                following
                privacyconfig
                postsliked
                postsshared
                
            
            PrivacyConfig:
                profilevisibility
                privatemessages
                searchvisibility
        
        
    
    POSTINGS:
        MODELS:
            abstract Post:
                user
                timestamp
                content
                likes
                shared
                

            MainPost(Post):
                commentaries

            Commentary(Post):
                mainpost
            
--------------------------------------------------------------------------
ENDPOINTS

/accounts/login/ (POST)
    -username
    -email
    -password

    -Returns Token key


/accounts/logout/ (POST)


/accounts/password/reset/ (POST)
    -email


/accounts/password/reset/confirm/ (POST)
    -uid (url parameter)
    -token (url parameter)
    -new_password1
    -new_password2


/accounts/password/change/ (POST)
    -new_password1
    -new_password2
    -old_password


/accounts/user/ (GET, PUT, PATCH)
    -username
    -first_name
    -last_name
    
    -Returns pk, username, email, first_name, last_name


/accounts/registration/ (POST)
    -username
    -password1
    -password2
    -email


/accounts/registration/verify-email/ (POST)
    -key

